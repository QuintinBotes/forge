# Agent Operating Rules

Agents working in this repository must write tests before implementation, keep
every change scoped to a single package, and never deploy to production without
an approved spec and an explicit human approval. Secrets must never be written
to logs, traces, or retrieval results.
