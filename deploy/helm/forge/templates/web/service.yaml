{{- if .Values.web.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "forge.fullname" . }}-web
  labels:
    {{- include "forge.componentLabels" (dict "ctx" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "forge.componentSelectorLabels" (dict "ctx" . "component" "web") | nindent 4 }}
{{- end }}
