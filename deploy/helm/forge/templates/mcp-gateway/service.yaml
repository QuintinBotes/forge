{{- if .Values.mcpGateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "forge.fullname" . }}-mcp-gateway
  labels:
    {{- include "forge.componentLabels" (dict "ctx" . "component" "mcp-gateway") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.mcpGateway.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "forge.componentSelectorLabels" (dict "ctx" . "component" "mcp-gateway") | nindent 4 }}
{{- end }}
